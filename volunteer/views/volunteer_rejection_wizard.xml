<odoo>
    <record id="view_volunteer_rejection_wizard_form" model="ir.ui.view">
        <field name="name">volunteer.rejection.wizard.form</field>
        <field name="model">volunteer.rejection.wizard</field>
        <field name="arch" type="xml">
            <form string="Reject Volunteer">
                <group>
                    <field name="rejection_reason"/>
                </group>
                <footer>
                    <button string="Reject" type="object" name="action_reject" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_volunteer_rejection_wizard" model="ir.actions.act_window">
        <field name="name">Volunteer Rejection</field>
        <field name="res_model">volunteer.rejection.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_volunteer_rejection_wizard_form"/>
        <field name="target">new</field>
    </record>
</odoo>
